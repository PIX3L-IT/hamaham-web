<!DOCTYPE html>
<html>
<head>
  <title>Panel de Permisos</title>
</head>
<body>
  <h1>Panel de control</h1>

  <section>
    <h2>Administración de usuarios</h2>
    <% if (adminUsers) { %>
      <p>Bienvenido al panel de administración de usuarios.</p>
    <% } else { %>
      <p><strong>No tienes permiso para ver esta sección.</strong></p>
    <% } %>
  </section>

  <section>
    <h2>Facturación</h2>
    <% if (bills) { %>
      <p>Acceso a funciones de facturación.</p>
    <% } else { %>
      <p><strong>No tienes permiso para ver esta sección.</strong></p>
    <% } %>
  </section>

  <section>
    <h2>Pacientes</h2>
    <% if (patients) { %>
      <p>Gestión de información de pacientes.</p>
    <% } else { %>
      <p><strong>No tienes permiso para ver esta sección.</strong></p>
    <% } %>
  </section>

  <section>
    <h2>Estadísticas</h2>
    <% if (statistics) { %>
      <p>Visualización de estadísticas del sistema.</p>
    <% } else { %>
      <p><strong>No tienes permiso para ver esta sección.</strong></p>
    <% } %>
  </section>
</body>
</html>
